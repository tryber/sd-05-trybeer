import React from 'react';
import AdminOrderCard from '../components/AdminOrderCard';

const AdminOrdersDetails= () => {
  this.setState({card: AdminOrderCard})

  return (
    <section>
    <span>
      {this.state.card}
    </span>
    </section>
  )
}

export default AdminOrdersDetails;
